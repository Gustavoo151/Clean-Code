package org.example.cap1;

public class UseNomePassiveisDeBuscas {
/*    Exemplo de código que não é claro, pois não sabemos o que é t e s, e o que eles representam.
      for (int j=0; j<34; j++){
          s += (t[j] *4)/5;
    }
*/
    /* Exemplo de código que é claro, pois sabemos o que é t e s, e o que eles representam.
    int  realDaysPerIdealDay = 4;
    const int WORK_DAYS_PER_WEEK = 5;
    int sum = 0;

    for (int j=0; j < NUMBER_OF_TASK; j++){
        int realTaskDay = taskEstimete[j] * realDaysPerIdealDay;
        int realTaskWeek = (realDays / WORK_DAYS_PER_WEEK);
        sum += realTaskWeek;
    }
   */
}
